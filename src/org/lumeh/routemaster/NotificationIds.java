package org.lumeh.routemaster;

/**
 * Centralize notification IDs passed to NotificationManager.notify. These must
 * be unique and non-zero for each notification.
 */
public class NotificationIds {
    public static final int TRACKING_SERVICE = 1;
}
