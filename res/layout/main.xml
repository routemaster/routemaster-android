<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              android:background="@color/activityBackground">
  <android.support.v7.widget.Toolbar
      android:id="@+id/toolbar"
      app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
      android:background="@color/primary"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"/>
  <com.astuetz.PagerSlidingTabStrip
      android:layout_gravity="top"
      android:layout_width="match_parent"
      android:layout_height="48dp"
      android:id="@+id/main_tabs"
      android:background="@color/primary"
      android:textColor="@color/actionBarForeground"
      app:pstsIndicatorColor="@color/actionBarForeground"
      app:pstsUnderlineColor="@color/actionBarForeground"
      app:pstsShouldExpand="true"/>
  <android.support.v4.view.ViewPager android:layout_gravity="center"
                                     android:layout_height="60dp"
                                     android:layout_width="match_parent"
                                     android:layout_weight="1"
                                     android:id="@+id/main"/>
</LinearLayout>
